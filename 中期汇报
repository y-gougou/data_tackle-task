# 中期汇报报告

**课题名称**：基于多传感器融合的机器人故障诊断研究

**学生姓名**：杨鹏

**所在院校**：燕山大学

**专业年级**：自动化专业 2022级本科

**汇报日期**：2026年4月21日

---

## 一、任务书中本阶段工作目标与任务要求

### 1.1 毕业设计总体任务概述

本毕业设计课题旨在研究基于多传感器融合的机器人故障诊断技术，以R550PLUS三全向轮机器人为研究对象，设计并实现一套完整的机器人故障诊断系统。根据任务书要求，本课题主要包含三大核心任务：

**任务一：故障诊断硬件平台设计**（权重30%）
完成故障情况下的电压、电流、转速、振动及加速度等多源传感器数据的采集与传输系统设计与实现。

**任务二：深度神经网络模型设计**（权重40%）
设计基于多传感器融合的机器人智能故障诊断深度神经网络模型，在自采数据集上完成神经网络的训练和测试。

**任务三：前端界面设计**（权重30%）
设计前端界面，完成机器运行数据、诊断结果的可视化，实现人机交互UI界面设计。

### 1.2 本阶段工作目标与任务要求

根据课题研究内容和进度安排，中期检查前应完成以下工作目标：

#### 1.2.1 硬件平台搭建目标

本阶段的核心目标是建立完整的机器人多传感器数据采集系统，具体包括：

（1）建立ROS（机器人操作系统）通信框架，实现各传感器数据的稳定采集与传输；

（2）实现底盘控制器与上位机之间的串口通信，采集里程计、IMU、电池电压等数据；

（3）设计并实现电流检测板与主控系统的通信协议，采集三通道电流数据；

（4）开发数据记录软件，实现多传感器数据的同步存储与标注；

（5）针对四种典型故障类型（正常状态、驱动异常、轮子打滑、电机轴偏心），分别进行数据采集实验。

#### 1.2.2 数据处理流水线目标

本阶段需要建立完整的数据预处理流水线，包括：

（1）实现多频率传感器数据的时间同步处理；

（2）实现缺失值检测与插值填充；

（3）实现异常值检测与剔除（基于3σ原则）；

（4）设计并实现多通道归一化方案；

（5）实现滑动窗口分割算法，将连续时序数据转换为样本；

（6）建立数据集验证流程，确保数据质量满足深度学习要求。

#### 1.2.3 远程控制系统目标

为方便数据采集和故障模拟，需要实现：

（1）开发基于Web的远程控制系统，实现机器人远程操控；

（2）实现多种输入方式支持（虚拟摇杆、键盘、WASD、Xbox手柄）；

（3）设计并实现PID巡航控制功能；

（4）建立完善的安全保护机制（指令超时、心跳检测、紧急停止）。

### 1.3 预期故障类型定义

根据机器人实际运行中可能出现的故障情况，任务书要求设计以下四类故障诊断标签：

| 标签 | 故障名称 | 故障描述 | 典型信号特征 |
|------|----------|----------|--------------|
| 0 | normal | 正常状态 | 各项参数稳定在正常范围内 |
| 1 | drive_fault | 驱动异常 | 单轮堵转时对应通道电流突变增大 |
| 2 | wheel_slip | 轮子打滑 | 轮毂脱落时速度波动增大、加速度异常 |
| 3 | shaft_eccentric | 电机轴偏心 | 周期性振动、角速度周期性波动 |

---

## 二、目前已完成任务情况

### 2.1 数据采集系统完成情况

#### 2.1.1 硬件系统架构

本课题已成功搭建完整的多传感器数据采集硬件系统。系统由两大部分组成：底盘控制器（STM32微控制器）和电流检测板（独立MCU）。两者通过USB转串口与运行ROS的上位机进行通信。

**底盘控制器系统**：
底盘控制器负责采集机器人的核心传感器数据，包括里程计信息（位置和速度）、IMU数据（六轴惯性测量单元，包含三轴加速度和三轴角速度）、以及电池电压监测。底盘控制器通过USB CDC协议将数据以20Hz的频率发送至上位机，使用设备路径`/dev/ttyCH343USB0`，波特率为115200。

**电流检测板系统**：
电流检测板采用独立MCU设计，负责采集机器人三通道电机驱动电流。该板卡通过串口协议以约5-6Hz的频率向上位机发送数据，使用设备路径`/dev/ttyUSB0`，波特率同样为115200。电流检测板采用文本协议传输数据，协议格式为：`$CURRENT,<ch0>,<ch1>,<ch2>*<checksum>\r\n`，其中checksum为XOR校验和，确保数据传输的可靠性。

#### 2.1.2 ROS话题通信架构

本课题已建立完整的ROS话题通信架构，各传感器数据通过独立的话题进行传输：

| 话题名称 | 消息类型 | 频率 | 数据内容 |
|----------|----------|------|----------|
| `/odom` | nav_msgs/Odometry | 20Hz | 位置(x,y,z)、速度(vx,vy,vz)、四元数方向 |
| `/imu` | sensor_msgs/Imu | 20Hz | 线加速度(ax,ay,az)、角速度(gx,gy,gz) |
| `/PowerVoltage` | std_msgs/Float32 | 20Hz | 电池电压（18-26V范围） |
| `/current_data` | std_msgs/Float32MultiArray | ~5-6Hz | 三通道电流值[current0,current1,current2] |
| `/cmd_vel` | geometry_msgs/Twist | 40Hz | 期望速度指令（线性/角速度） |

#### 2.1.3 数据采集实现

本课题开发了专门的数据采集节点`data_collector.py`，采用触发式采集机制实现多传感器数据的同步记录。该节点以`/odom`话题（20Hz）作为主触发源，每当收到一条里程计消息时，即记录一行包含所有传感器数据的完整记录。对于频率较低的电流数据（5-6Hz），采用"最新值"策略进行填充，确保数据记录的连续性和均匀性。

CSV输出文件包含19个字段，完整记录了时间戳、各传感器原始数据、计算特征以及故障标签。文件格式为：
```
timestamp,frame,odom_time,imu_time,voltage_time,current_time,
x,y,z,vx,vy,vz,ax,ay,az,gx,gy,gz,
voltage,current0,current1,current2,fault_label
```

#### 2.1.4 故障数据采集情况

截至目前，已完成四类故障数据的采集工作：

| 故障类型 | 标签 | 采集日期 | 原始行数 | 窗口数 | 状态 |
|----------|------|----------|----------|--------|------|
| 正常状态 | 0 | 4月19日 | ~12,000 | 110 | ✅已完成 |
| 驱动异常 | 1 | 4月19日 | ~3,500 | 43 | ✅已完成 |
| 轮子打滑 | 2 | 4月20日 | ~1,500 | 19 | ✅已完成 |
| 电机轴偏心 | 3 | 4月20日 | ~1,300 | 17 | ✅已完成 |

**数据集统计**：总计采集原始数据约18,300行，经过滑动窗口分割（窗口大小100点，步长50点，50%重叠率），共获得189+个有效样本。

### 2.2 数据处理流水线完成情况

#### 2.2.1 时间同步处理

针对`/current_data`话题频率（5-6Hz）与其他话题（20Hz）不匹配的问题，本课题设计并实现了基于线性插值的时间同步算法。该算法以高频话题的时间轴为基准，对低频通道进行插值，将电流数据对齐到20Hz的时间轴上。插值后的数据消除了大量重复值问题，为后续特征提取提供了更精确的时间对齐。

#### 2.2.2 缺失值处理

数据采集过程中可能因通信抖动或传感器瞬时故障导致数据缺失。本课题采用线性插值方法对缺失值进行填充，具体实现中使用了pandas库的`interpolate`方法，设置`limit_direction='both'`参数确保插值方向灵活性。处理后的数据集经检验无缺失值，满足深度学习训练要求。

#### 2.2.3 异常值剔除

本课题采用统计学3σ原则进行异常值检测与剔除。该方法基于正态分布特性，数据落在[μ-3σ, μ+3σ]区间内的概率为99.7%。对于每个特征通道，计算其均值和标准差，将超出该范围的数据点标记为异常并剔除。实际处理中异常点比例约为0.01%，表明原始数据质量较高。

#### 2.2.4 归一化处理

本课题设计了混合归一化策略以适应不同类型传感器的数据特性：

| 通道类型 | 归一化方法 | 输出范围 | 原因 |
|----------|------------|----------|------|
| 电压 | Min-Max | [0, 1] | 单极性数据，始终为正 |
| 位置/速度/加速度/角速度/电流 | 对称归一化 | [-1, 1] | 有正负值，需保留方向信息 |

对称归一化公式为：`x_norm = x / max_abs`，其中`max_abs`为该通道数据的最大绝对值。

#### 2.2.5 滑动窗口分割

本课题实现了两阶段滑动窗口分割算法。参数设置为：窗口大小100点（对应5秒@20Hz），步长50点（对应2.5秒），重叠率50%。窗口标签采用众数策略，取窗口内出现次数最多的故障标签作为整个窗口的标签。

训练测试集划分采用时序划分方法（前80%为训练集，后20%为测试集），避免使用随机划分导致的数据泄露问题。

#### 2.2.6 数据集验证

本课题开发了完整的数据集验证流程，包括：

- Shape检查：确保所有样本维度一致
- 数据类型检查：确保数据类型正确
- 缺失值检查：确保无缺失数据
- 无穷值检查：确保无异常数值
- 标签范围检查：确保标签值在有效范围内
- 数值范围检查：确保归一化后数据在预期范围内

### 2.3 Web遥控系统完成情况

#### 2.3.1 系统架构

本课题成功实现了基于Web的机器人远程控制系统，系统架构如下：

浏览器端通过WebSocket协议与Rosbridge服务器建立连接，将用户输入（虚拟摇杆、键盘、WASD、Xbox手柄）转换为ROS的`geometry_msgs/Twist`消息并发送至`/cmd_vel_web`话题。服务端`cmd_vel_web_adapter.py`节点订阅该话题，进行处理后转发至`/cmd_vel`话题控制机器人运动。

#### 2.3.2 多种输入方式支持

系统支持四种输入方式：

（1）**虚拟摇杆**：基于HTML5 Canvas开发，支持触摸操作，可同时控制前进/后退和转向；

（2）**键盘控制**：WASD键控制前后左右移动，Q/E键控制转向；

（3）**Xbox手柄**：通过Gamepad API支持标准游戏手柄，提供最自然的操控体验；

（4）**直接指令**：可通过ROS话题直接发送速度指令。

#### 2.3.3 响应曲线处理

为改善操控手感，系统实现了非线性响应曲线处理：

```python
输出 = 最小比例 + (1 - 最小比例) × 输入^指数
```

该设计确保即使在低输入情况下也有基本响应（避免死区），同时通过指数调整曲线形状，实现精细控制。默认参数：死区0.07，最小输出比例0.2，指数0.7。

#### 2.3.4 PID巡航控制

系统实现了三通道独立PID控制器，用于定速巡航控制。PID参数：Kp=0.8，Ki=0.1，Kd=0.3，输出限幅±1.0，积分限幅±0.5。巡航控制可通过`/web/cruise_enable`话题激活，通过`/web/cruise_cmd`话题设置目标速度。

#### 2.3.5 安全保护机制

系统实现了五层安全保护机制：

| 机制 | 触发条件 | 行为 | 恢复方式 |
|------|----------|------|----------|
| 指令超时 | 0.5秒无新指令 | 自动停车 | 发送新指令 |
| 心跳超时 | 1.0秒无心跳 | 自动停车 | 发送新指令 |
| 紧急停止 | 收到estop命令 | 锁定停车 | 手动解除 |
| 速度限制 | 任何指令 | 限制在最大值内（线性x: ±1.5m/s, 线性y: ±1.0m/s, 角速度z: ±3.75rad/s） | 始终生效，无需恢复 |
| 巡航优先 | 巡航模式激活 | 忽略摇杆输入 | 关闭巡航 |

### 2.4 技术文档完成情况

本课题已完成以下技术文档的编写：

（1）**代码实现说明**：详细记录了数据采集、Web控制、数据预处理三大模块的实现细节，共计16章节；

（2）**中期总结报告**：完整记录了项目概述、已完成工作、数据集分析、存在问题、技术方案设计、下一步计划等内容；

（3）**关键代码注释**：所有Python节点代码均包含详细的中文注释和函数文档字符串。

---

## 三、存在的问题和拟解决方法

### 3.1 数据层面的问题

#### 3.1.1 类别不平衡问题

**问题描述**：
当前数据集中各类别样本分布极不均衡。正常状态（标签0）有110个窗口，占比58.2%；而轮子打滑（标签2）仅有19个窗口，占比10.1%，电机轴偏心（标签3）仅有17个窗口，占比9.0%。这种严重的类别不平衡会导致训练出的模型严重偏向预测多数类，对少数类的召回率极低。

**问题原因**：
故障数据采集相对困难，特别是轮子打滑和电机轴偏心故障需要人工模拟，且存在一定危险性。此外，采集过程耗时较长，难以获得足够的样本。

**拟解决方法**：
（1）**加权损失函数**：在模型训练时采用带类别权重的交叉熵损失函数，对少数类赋予更高权重。权重计算公式为样本数倒数归一化后的值；

（2）**数据增强**：对少数类样本采用时序抖动、幅度缩放、高斯噪声等增强方法，将每个样本扩充至4-8倍；

（3）**SMOTE过采样**：采用SMOTE（合成少数类过采样技术）生成更多少数类合成样本；

（4）**补充采集**：在后续时间继续补充采集少数类故障数据，特别是轮子打滑和电机轴偏心样本。

#### 3.1.2 样本总量不足

**问题描述**：
当前仅有189个有效样本，而深度学习模型训练通常需要500以上的样本量才能获得较好的泛化能力。样本总量不足会导致模型容易过拟合，在新数据上的表现可能较差。

**问题原因**：
每次数据采集需要人工值守，且故障模拟需要较多准备时间。现有采集工作仅进行了2天（4月19-20日），时间有限。

**拟解决方法**：
（1）制定详细的采集计划，每日采集3-4小时，目标在2周内将总样本量提升至400+；

（2）优化采集流程，减少人工干预时间，提高采集效率；

（3）利用数据增强技术将实际样本扩充至4-5倍，有效样本量可达700+；

（4）在模型训练阶段采用交叉验证方法，充分利用现有数据。

#### 3.1.3 数据来源单一

**问题描述**：
所有数据均来自2天的采集（4月19-20日），采集条件相对固定（相同环境、相同负载、相同实验人员），缺乏数据多样性。

**问题原因**：
采集时间窗口有限，未能进行多日、多条件的数据采集。

**拟解决方法**：
（1）在不同日期进行补充采集，引入时间多样性；

（2）在不同负载条件下进行采集（空载、半载、满载）；

（3）考虑在不同地面上采集（平滑地面、粗糙地面、有水地面等）；

（4）在不同温度环境下进行测试。

#### 3.1.4 缺少转速和振动传感器

**问题描述**：
任务书要求采集"转速、振动"等传感器数据，但当前系统仅采集了电流、电压、IMU加速度和角速度数据，未直接采集电机转速和振动信号。

**问题原因**：
R550PLUS机器人底盘控制器的固件未暴露电机转速数据接口，振动传感器需要额外硬件采购和安装。

**拟解决方法**：
（1）**间接估计**：通过里程计数据计算轮子转速，作为替代方案；

（2）**固件扩展**：联系WHEELTEC厂商，询问是否可获取电机转速数据；

（3）**硬件扩展**：采购三轴振动传感器模块，通过Arduino采集后通过ROS话题发布；

（4）**特征工程**：通过电流数据的时域和频域特征间接反映振动信息。

### 3.2 算法层面的问题

#### 3.2.1 故障诊断模型未设计

**问题描述**：
作为毕设的核心任务（权重40%），故障诊断深度神经网络模型目前尚未开始设计。已完成的189个样本窗口目前仅完成数据准备工作，无法进行实际的故障诊断。

**问题原因**：
前期工作重点放在了数据采集和预处理流水线上，模型设计工作尚未启动。

**拟解决方法**：
（1）**模型选型**：综合考虑时序数据处理能力和硬件资源限制，推荐采用1D-CNN + LSTM混合架构，该架构结合了CNN的局部特征提取能力和LSTM的长时序依赖建模能力；

（2）**输入设计**：输入规格为(batch_size, 100, 16)，其中100为时间步数（5秒@20Hz），16为特征数；

（3）**初步架构**：
  - 1D-CNN特征提取器：三层卷积（64→128→256通道），每层后接BatchNorm、ReLU和MaxPool
  - LSTM层：两层LSTM，hidden_size=128
  - 全连接分类器：128→64→5

（4）**实现计划**：在接下来的一周内完成PyTorch模型代码编写和初步训练验证。

#### 3.2.2 推理节点未开发

**问题描述**：
即使完成了模型训练，也需要开发ROS推理节点来实现实时故障诊断。目前该节点完全未开发。

**问题原因**：
依赖于模型训练完成，属于下游任务。

**拟解决方法**：
（1）设计inference_node.py节点，实现以下功能：
  - 订阅`/odom`、`/imu`、`/current_data`话题
  - 维护5秒（100点）的环形数据缓冲区
  - 调用训练好的模型进行推理
  - 发布诊断结果至`/diagnosis/result`话题

（2）节点需包含完整的归一化处理流程，使用与训练时相同的归一化参数；

（3）在5月中旬前完成节点开发和基础功能测试。

#### 3.2.3 模型训练流程未建立

**问题描述**：
缺乏完整的模型训练、验证、测试流程，包括训练脚本、超参数调优、交叉验证、模型保存等环节。

**问题原因**：
依赖于数据集完善和模型设计。

**拟解决方法**：
（1）开发train.py训练脚本，实现以下功能：
  - 数据加载器（DataLoader）
  - 训练循环（多epoch）
  - 验证循环（每个epoch后）
  - 学习率调度（ReduceLROnPlateau）
  - 早停机制（EarlyStopping）
  - 模型保存（最佳模型checkpoint）

（2）训练配置：
  - Batch size: 16
  - Epochs: 100
  - 学习率: 1e-3
  - 优化器: Adam
  - 损失函数: 带类别权重的交叉熵

（3）建立超参数调优流程，重点调优学习率、batch size、LSTM hidden size等关键参数。

### 3.3 系统层面的问题

#### 3.3.1 诊断结果展示UI缺失

**问题描述**：
当前Web控制界面仅有遥控功能，缺乏故障诊断结果的实时展示。这直接影响了毕设任务三（前端界面设计）的完成度。

**问题原因**：
Web开发资源有限，优先完成了基础的遥控功能。

**拟解决方法**：
（1）扩展Web控制界面，新增诊断结果面板；

（2）设计诊断卡片组件，显示：
  - 当前诊断结果（故障类型名称）
  - 置信度百分比及进度条
  - 各类型概率分布条形图

（3）设计历史记录功能，保存最近100条诊断记录；

（4）计划在5月底前完成UI开发和集成。

#### 3.3.2 报警系统缺失

**问题描述**：
当机器人发生故障时，系统无法主动提醒用户，可能导致故障被忽视而造成进一步损坏。

**问题原因**：
属于锦上添花的功能，优先级较低。

**拟解决方法**：
（1）实现置信度阈值报警：当诊断置信度>80%且故障类型非正常时触发报警；

（2）报警方式：
  - 视觉报警：红色警示框、闪烁效果
  - 声音报警：播放报警音效
  - 消息推送：可选的邮件/短信通知

（3）报警确认机制：用户确认后消除报警，避免持续干扰；

（4）在5月底前完成报警系统开发。

#### 3.3.3 控制权限管理缺失

**问题描述**：
当前系统缺乏多用户权限管理，多个用户可能同时控制机器人，存在安全隐患。

**问题原因**：
属于高级功能，基础遥控功能已满足当前需求。

**拟解决方法**：
（1）实现简单的令牌机制：获取控制令牌后才能发送控制指令；

（2）设计超时释放：一定时间无操作自动释放控制令牌；

（3）在后续版本考虑WebSocket连接管理和多客户端协调。

### 3.4 时间风险评估

| 风险项 | 概率 | 影响程度 | 优先级 | 应对策略 |
|--------|------|----------|--------|----------|
| 样本量不足导致模型效果差 | 高 | 高 | P1 | 增加采集时间，采用数据增强 |
| 时间不足导致任务无法完成 | 中 | 高 | P2 | 压缩开发周期，并行推进 |
| 模型过拟合 | 中 | 中 | P3 | 正则化，数据增强，交叉验证 |
| 少数类召回率极低 | 高 | 中 | P4 | 加权损失，SMOTE，补充采集 |

### 3.5 下一步工作计划

基于上述问题分析，制定以下详细工作计划：

**第一阶段：数据补充（第1-2周，4月21日-5月4日）**
- 补充采集各类故障数据，目标每类50+样本
- 增加数据多样性（不同日期、不同负载）

**第二阶段：模型开发（第3周，5月5日-5月11日）**
- 完成PyTorch训练框架搭建
- 实现1D-CNN+LSTM模型
- 实现数据增强和类别平衡策略

**第三阶段：模型训练（第4周，5月12日-5月18日）**
- 完成首次训练和验证
- 超参数调优
- 交叉验证评估
- 保存最佳模型

**第四阶段：推理节点（第5周，5月19日-5月25日）**
- 开发inference_node.py
- 集成到ROS系统
- 实时诊断测试

**第五阶段：前端UI（第6周，5月26日-6月1日）**
- 诊断结果展示组件
- 报警系统
- 故障历史记录

**第六阶段：系统联调与论文（第7-10周，6月2日-6月30日）**
- 端到端测试
- 性能优化
- 论文撰写
- PPT制作
- 答辩准备

---

## 四、结论

本次中期汇报全面总结了本课题的研究进展。目前已完成了故障诊断硬件平台的数据采集系统设计和实现，包括多传感器ROS通信、串口协议解析、数据采集节点开发等核心功能；同时完成了数据处理流水线的搭建，实现了时间同步、缺失值处理、异常值剔除、归一化、滑动窗口分割等完整的数据预处理流程；此外，还实现了功能完备的Web远程控制系统，支持多种输入方式和安全保护机制。

在存在的问题方面，主要集中在数据层面（类别不平衡、样本不足、多样性单一）和算法层面（模型未设计、推理节点未开发）。针对这些问题，本报告提出了详细的解决方案和下一阶段工作计划。

总体而言，本课题按照任务书要求稳步推进，完成了预定的阶段性目标。后续将在此基础上，重点推进深度学习模型设计和实现工作，确保毕设任务顺利完成。

---

**汇报人**：杨鹏

**汇报日期**：2026年4月21日
